# harness-bootstrap >>>
# HYG-03. The documented shape of the environment, with NO real value.
#
# Este projeto não usa variável de ambiente: é bash e markdown, sem serviço, sem
# credencial, sem chamada de API. O arquivo fica como contrato — se um dia um script
# precisar de configuração, ela é declarada aqui (nome, para que serve, onde obter) e
# o valor real vai para `.env`, que é gitignored.
#
# Ausência declarada vale mais que arquivo com exemplo inventado.
# harness-bootstrap <<<
